#coding:utf-8
data = [45,'user',{'AdminRoot':'Ambroise'}, {'AdminLanguage':['Python','CPython','JPython']}]


def dateRecover(data):
    for  i in range(len(data)) :
        if i == 3 :
            for  v in enumerate(data[i].items()) :
                for x in list(v[1][1]) :
                    print(x)

